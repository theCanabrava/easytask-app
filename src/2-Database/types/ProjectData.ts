export default interface ProjectData
{
    id: string,
    projectName?: string,
    description?: string,
    startDate?: string,
    finishDate?: string,
    managerId?: string,
    completed?: boolean,
}